<form method="post" action="{{ path('quizs_delete', {'idQuizs': quiz.idQuizs}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ quiz.idQuizs) }}">
    <button class="btn btn-primary">Delete</button>
</form>



